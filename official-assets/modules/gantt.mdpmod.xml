<module>
  <name>gantt</name>
  <type>block</type>
  <description>Simple Gantt chart — tasks as bars over a shared time track (pure CSS grid). Body = one task per section, "Name | start end".</description>
  <tags>timeline, charts</tags>
  <aiSpec><![CDATA[
A simple GANTT / schedule chart — tasks drawn as bars on a shared timeline.

Body: split the sections with `<!-- @ -->`, one task per section written
`Name | start end` — start and end are 1-based unit numbers, inclusive (so `1 3`
spans units 1 through 3). A task with start == end shows a diamond milestone. The
number of columns auto-fits the latest end unless you set `units`.

When NOT to use: for dependency arrows / critical paths use @mermaid gantt; this is a
lightweight roadmap view.

Parameters:
  - `units`   : number of time-unit columns. Blank = auto (fits the latest end).
                Whole number >= 1.
  - `labels`  : comma-separated column names (e.g. week names). Blank = numbers 1..n.
  - `color`   : bar and column-header color. Default var(--accent-color).
  - `gap`     : vertical gap between rows in px. Default 6. Range 0–40.
  - `barradius` : bar corner radius in px. Default 4. Range 0–20.
  - `barheight` : bar height in em. Default 1.5. Range 0.8–4.
  - `showduration` : true/false — print the duration (unit count) inside each bar.
                     Default false.

<!-- @gantt units: 6, labels: W1, W2, W3, W4, W5, W6, showduration: true -->
Research | 1 2
<!-- @ -->
Design | 2 4
<!-- @ -->
Build | 3 6
<!-- @ -->
Launch | 6 6
<!-- @end -->
]]></aiSpec>
  <parameters>
    <param name="units" type="number" default="" min="1" integer="true" label="Columns (time units)" desc="Number of time-unit columns; blank = auto (fits the latest end)." />
    <param name="labels" type="text" default="" label="Column labels" desc="Comma-separated column names; blank = numbers 1..n." />
    <param name="color" type="color" default="var(--accent-color)" label="Bar color" desc="Bar and column-header color." />
    <param name="gap" type="number" default="6" min="0" max="40" step="1" label="Row gap (px)" desc="Vertical gap between task rows." />
    <param name="barradius" type="number" default="4" min="0" max="20" step="1" label="Bar radius (px)" desc="Corner radius of the task bars." />
    <param name="barheight" type="number" default="1.5" min="0.8" max="4" step="0.1" label="Bar height (em)" desc="Height of the task bars, in em." />
    <param name="showduration" type="boolean" default="false" label="Show duration" desc="Print the duration (unit count) inside each bar." />
  </parameters>
  <snippets>
    <snippet>
      <category>Diagrams</category>
      <label>Gantt chart</label>
      <text><![CDATA[<!-- @gantt units: 6, labels: W1, W2, W3, W4, W5, W6 -->
Research | 1 2
<!-- @ -->
Design | 2 4
<!-- @ -->
Build | 3 6
<!-- @ -->
Launch | 6 6
<!-- @end -->]]></text>
      <description>Tasks as bars over a shared time track. "Name | start end" (1-based, inclusive).</description>
    </snippet>
  </snippets>
  <render><![CDATA[
    const color = args.color || 'var(--accent-color)';
    const esc = (s) => String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
    const gap = Math.max(0, Math.min(40, Number.isFinite(parseFloat(args.gap)) ? parseFloat(args.gap) : 6));
    const br = Math.max(0, Math.min(20, Number.isFinite(parseFloat(args.barradius)) ? parseFloat(args.barradius) : 4));
    const bh = Math.max(0.8, Math.min(4, Number.isFinite(parseFloat(args.barheight)) ? parseFloat(args.barheight) : 1.5));
    const showDur = String(args.showduration) === 'true';
    const tasks = [];
    sections.forEach((s) => {
      const m = String(s || '').trim().match(/^([\s\S]*?)[|:]\s*(\d+)\s*[-– ]\s*(\d+)\s*$/);
      if (!m) return;
      const a = parseInt(m[2], 10), b = parseInt(m[3], 10);
      tasks.push({ name: m[1].trim(), start: Math.min(a, b), end: Math.max(a, b) });
    });
    if (!tasks.length) return `<div class="mdp-mod-gantt">(each section must be "Name | start end")</div>`;
    let units = parseInt(args.units, 10) || 0;
    if (!units) units = Math.max(...tasks.map((t) => t.end), 1);
    const colLabels = String(args.labels || '').split(',').map((x) => x.trim()).filter(Boolean);
    let head = '';
    if (colLabels.length) {
      head = `<div class="mdp-gt-row mdp-gt-head"><div class="mdp-gt-name"></div><div class="mdp-gt-track" style="grid-template-columns:repeat(${units},1fr);">` +
        Array.from({ length: units }, (_, i) => `<div class="mdp-gt-col">${esc(colLabels[i] || (i + 1))}</div>`).join('') + `</div></div>`;
    }
    let rows = '';
    tasks.forEach((t) => {
      const dur = t.end - t.start + 1;
      const inner = showDur ? esc(dur) : (t.start === t.end ? '◆' : '');
      rows += `<div class="mdp-gt-row"><div class="mdp-gt-name">${esc(t.name)}</div>` +
        `<div class="mdp-gt-track" style="grid-template-columns:repeat(${units},1fr);">` +
        `<div class="mdp-gt-bar" style="grid-column:${t.start} / ${t.end + 1};">${inner}</div></div></div>`;
    });
    return `\n\n<div class="mdp-mod-gantt" style="--gt-color:${color}; --gt-radius:${br}px; --gt-barh:${bh}em; gap:${gap}px;">${head}${rows}</div>\n\n`;
  ]]></render>
  <style><![CDATA[
    .mdp-mod-gantt { margin: 1rem 0; display: flex; flex-direction: column; gap: 6px; font-size: 0.9em; }
    .mdp-gt-row { display: grid; grid-template-columns: minmax(5em, auto) 1fr; align-items: center; gap: 10px; }
    .mdp-gt-name { text-align: right; font-weight: 600; }
    .mdp-gt-track { display: grid; gap: 0; background: color-mix(in srgb, var(--gt-color) 8%, transparent); border-radius: 4px; }
    .mdp-gt-head .mdp-gt-track { background: none; }
    .mdp-gt-col { text-align: center; font-size: 0.8em; color: var(--gt-color); padding: 2px 0; border-left: 1px solid color-mix(in srgb, var(--gt-color) 20%, transparent); }
    .mdp-gt-bar {
      background: var(--gt-color); color: #fff; border-radius: var(--gt-radius, 4px); height: var(--gt-barh, 1.5em);
      display: flex; align-items: center; justify-content: center; font-size: 0.8em; min-width: 0;
    }
  ]]></style>
</module>
