<module>
  <name>timeline</name>
  <type>block</type>
  <description>Displays multiple content blocks as a vertical timeline of numbered (or custom-labelled) steps.</description>
  <tags>timeline, process</tags>
  <aiSpec><![CDATA[
Use to present a sequence of steps / phases / milestones as a vertical timeline with
numbered (or custom-labelled) markers. Block module: split the body into steps with
`<!-- @ -->` — one section per step, in order. Each section is ordinary Markdown (a
heading + description works well). Do NOT use this for a flat bullet list; use it when the
order and progression matter.

Parameters:
  color         — marker/line accent colour (default var(--accent-color)).
  active_index  — which step is highlighted, 1-based (default 0 = none).
  indexes       — custom marker labels as a list, e.g. [Phase 1, Phase 2, Phase 3]
                  (default [] = auto 1,2,3...).
  markershape   — marker outline: pill (default) | rounded | square.
  cardbg        — background colour of each step card (default "#ffffff").
  gap           — vertical spacing between steps as a CSS length (default "2rem").
  connector     — draw the vertical line linking the markers (default true).

Example:
<!-- @timeline active_index: 2, color: #2563eb, indexes: [Plan, Build, Ship], gap: 2.5rem -->
### Plan
Interview users and scope the work.
<!-- @ -->
### Build
Agile development with continuous review.
<!-- @ -->
### Ship
Integration testing and release.
<!-- @end -->
]]></aiSpec>
  <parameters>
    <param name="color" type="color" default="var(--accent-color)" label="Color" desc="Accent colour of the markers and connecting line." />
    <param name="active_index" type="number" default="0" label="Active Step" desc="Which step is highlighted (1-based; 0 = none)." min="0" integer="true" />
    <param name="indexes" type="[text]" default="[]" label="Custom Labels" desc="Marker labels, e.g. [Phase 1, Phase 2, Phase 3]. Blank = automatic numbering." />
    <param name="markershape" type="select" default="pill" label="Marker Shape" desc="Outline shape of the step markers." options="pill:Pill,rounded:Rounded,square:Square" />
    <param name="cardbg" type="color" default="#ffffff" label="Card Background" desc="Background colour of each step's content card." />
    <param name="gap" type="text" default="2rem" label="Step Gap" desc="Vertical spacing between steps as a CSS length, e.g. 2rem or 24px." />
    <param name="connector" type="boolean" default="true" label="Connector Line" desc="Show the vertical line joining the markers." />
  </parameters>
  <snippets>
    <snippet>
      <category>Layout</category>
      <label>Timeline</label>
      <text><![CDATA[<!-- @timeline active_index: 2, indexes: [Phase1, Phase2, Phase3] -->
### Phase 1: Requirements & Research
Identify issues through user interviews.
<!-- @ -->
### Phase 2: System Design & Prototyping
Create screen mockups and gather feedback.
<!-- @ -->
### Phase 3: Development & QA
Proceed with agile development and conduct integration testing.
<!-- @end -->]]></text>
      <description>Converts multiple sections into a vertical step display with sequential numbers (or custom labels). Markdown is supported inside.</description>
    </snippet>
  </snippets>
  <render><![CDATA[
    const color = args.color || 'var(--accent-color)';
    const activeIndex = parseInt(args.active_index, 10) || 0;
    const cardBg = (args.cardbg || '#ffffff').trim() || '#ffffff';
    const gap = (args.gap || '2rem').trim() || '2rem';
    const shapeKey = String(args.markershape || 'pill').toLowerCase();
    const shape = ['pill', 'rounded', 'square'].includes(shapeKey) ? shapeKey : 'pill';
    const connector = String(args.connector) !== 'false';

    const indexesArr = Array.isArray(args.indexes) ? args.indexes : [];

    const wrapCls = `mdp-mod-timeline mdp-tl-mk-${shape}` + (connector ? '' : ' mdp-tl-noline');
    let html = `\n\n<div class="${wrapCls}" style="--timeline-color: ${color}; --timeline-card: ${cardBg}; --timeline-gap: ${gap};">\n`;

    sections.forEach((section, index) => {
       const contentStr = section.trim();
       if (!contentStr) return;

       const currentNum = index + 1;
       const label = indexesArr[index] !== undefined && indexesArr[index] !== '' ? indexesArr[index] : currentNum;
       const isActive = currentNum === activeIndex;
       const activeClass = isActive ? ' active' : '';

       html += `
<div class="mdp-timeline-item${activeClass}">
  <div class="mdp-timeline-marker${activeClass}">${label}</div>
  <div class="mdp-timeline-content${activeClass}">

${contentStr}

  </div>
</div>
`;
    });

    html += `</div>\n\n`;
    return html;
  ]]></render>
  <style><![CDATA[
    .mdp-mod-timeline {
      position: relative;
      margin: calc(1 * var(--mdp-u, 1rem)) 0;
      padding-left: calc(10 * var(--mdp-px, 1px));
    }

    .mdp-mod-timeline::before {
      content: '';
      position: absolute;
      top: calc(15 * var(--mdp-px, 1px));
      bottom: -15px;
      left: calc(32 * var(--mdp-px, 1px));
      width: calc(4 * var(--mdp-px, 1px));
      background: linear-gradient(to bottom, var(--timeline-color) 80%, transparent);
      opacity: 0.3;
      border-radius: calc(2 * var(--mdp-px, 1px));
      z-index: 0;
    }
    .mdp-tl-noline::before { display: none; }

    .mdp-timeline-item {
      position: relative;
      margin-bottom: var(--timeline-gap, calc(2 * var(--mdp-u, 1rem)));
      display: flex;
      align-items: flex-start;
      z-index: 1;
    }

    .mdp-timeline-item:last-child {
      margin-bottom: 0;
    }

    .mdp-timeline-marker {
      height: calc(44 * var(--mdp-px, 1px));
      min-width: calc(44 * var(--mdp-px, 1px));
      padding: 0 calc(1 * var(--mdp-u, 1rem));
      background-color: white;
      color: var(--timeline-color);
      border: calc(3 * var(--mdp-px, 1px)) solid var(--timeline-color);
      border-radius: calc(22 * var(--mdp-px, 1px));
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 900;
      box-shadow: 0 calc(4 * var(--mdp-px, 1px)) calc(8 * var(--mdp-px, 1px)) rgba(0,0,0,0.1);
      z-index: 2;
      margin-right: calc(24 * var(--mdp-px, 1px));
      margin-top: calc(4 * var(--mdp-px, 1px));
      white-space: nowrap;
      transition: transform 0.3s ease, background-color 0.3s, color 0.3s;
    }
    /* Marker outline shape. Pill reproduces the original calc(22 * var(--mdp-px, 1px)) radius. */
    .mdp-tl-mk-rounded .mdp-timeline-marker { border-radius: calc(8 * var(--mdp-px, 1px)); }
    .mdp-tl-mk-square .mdp-timeline-marker { border-radius: calc(3 * var(--mdp-px, 1px)); }

    .mdp-timeline-marker.active {
      background-color: var(--timeline-color);
      color: white;
      transform: scale(1.1);
      box-shadow: 0 calc(6 * var(--mdp-px, 1px)) calc(12 * var(--mdp-px, 1px)) rgba(0,0,0,0.2);
    }

    .mdp-timeline-content {
      background: var(--timeline-card, #ffffff);
      padding: calc(1.2 * var(--mdp-u, 1rem)) calc(1.5 * var(--mdp-u, 1rem));
      border-radius: calc(12 * var(--mdp-px, 1px));
      box-shadow: 0 calc(4 * var(--mdp-px, 1px)) calc(10 * var(--mdp-px, 1px)) rgba(0,0,0,0.05);
      border: calc(1 * var(--mdp-px, 1px)) solid #f0f0f0;
      width: 100%;
      color: #555;
      position: relative;
    }

    .mdp-timeline-content.active {
      color: #333;
      border-color: var(--timeline-color);
      box-shadow: 0 calc(6 * var(--mdp-px, 1px)) calc(15 * var(--mdp-px, 1px)) rgba(0,0,0,0.1);
    }

    .mdp-timeline-content > *:first-child { margin-top: 0; }
    .mdp-timeline-content > *:last-child { margin-bottom: 0; }
    .mdp-timeline-content h1,
    .mdp-timeline-content h2,
    .mdp-timeline-content h3 {
      color: var(--timeline-color);
      margin-top: 0;
      font-size: calc(1.3 * var(--mdp-u, 1rem));
      border-bottom: none;
      padding-bottom: 0;
    }

    .mdp-timeline-content::before {
      content: '';
      position: absolute;
      left: -8px;
      top: calc(20 * var(--mdp-px, 1px));
      width: calc(16 * var(--mdp-px, 1px));
      height: calc(16 * var(--mdp-px, 1px));
      background: var(--timeline-card, #ffffff);
      border-left: calc(1 * var(--mdp-px, 1px)) solid #f0f0f0;
      border-bottom: calc(1 * var(--mdp-px, 1px)) solid #f0f0f0;
      transform: rotate(45deg);
      transition: border-color 0.3s ease;
    }

    .mdp-timeline-content.active::before {
      border-left-color: var(--timeline-color);
      border-bottom-color: var(--timeline-color);
    }
  ]]></style>
</module>