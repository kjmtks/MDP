<module>
  <name>kbd</name>
  <type>block</type>
  <description>A keyboard key. The key label is the block body. Renders inline.</description>

  <parameters></parameters>

  <snippets>
    <snippet>
      <category>UI Parts</category>
      <label>Keyboard Key</label>
      <text><![CDATA[<!-- @kbd -->
Enter
<!-- @end -->]]></text>
      <description>A single physical keyboard key. Body = the key label.</description>
    </snippet>
  </snippets>

  <render inline="true"><![CDATA[
    const esc = (s) => String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    const body = sections[0] ? sections[0].trim() : '';
    return '<kbd class="mdp-mod-kbd">' + esc(body) + '</kbd>';
  ]]></render>

  <style><![CDATA[
    .mdp-mod-kbd {
      display: inline-block;
      font-family: ui-monospace, "Cascadia Code", "Segoe UI Mono", Menlo, Consolas, monospace;
      font-size: 0.85em;
      line-height: 1;
      padding: 3px 7px;
      color: var(--text-color, #1f2937);
      background-color: var(--inline-code-bg, #f3f4f6);
      border: 1px solid var(--border-color, #d1d5db);
      border-bottom-width: 2px;
      border-radius: 5px;
      box-shadow: var(--shadow-sm, 0 1px 0 rgba(0,0,0,0.15));
      white-space: nowrap;
      vertical-align: middle;
    }
  ]]></style>

  <script></script>
</module>
