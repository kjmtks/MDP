<module>
  <name>timer</name>
  <type>inline</type>
  <description>Countdown timer with start / pause / reset. Interactive: clicking it does not advance the slide. Auto-starts when presenting if autostart:true.</description>
  <interactive>true</interactive>

  <parameters>
    <param name="minutes" default="5" />
    <param name="seconds" default="0" />
    <param name="autostart" default="false" />
  </parameters>

  <snippets>
    <snippet>
      <category>Interactive</category>
      <label>Timer (5 min)</label>
      <text><![CDATA[<!-- @timer minutes: 5, autostart: false -->]]></text>
      <description>Countdown timer with start/pause/reset. Clicks don't advance the slide.</description>
    </snippet>
    <snippet>
      <category>Interactive</category>
      <label>Timer (auto-start on present)</label>
      <text><![CDATA[<!-- @timer minutes: 10, autostart: true -->]]></text>
      <description>Starts automatically when the slideshow opens this page.</description>
    </snippet>
  </snippets>

  <render><![CDATA[
    const mins = parseInt(args.minutes, 10) || 0;
    const secs = parseInt(args.seconds, 10) || 0;
    let total = mins * 60 + secs;
    if (!total) total = 300;
    const autostart = (args.autostart === 'true' || args.autostart === '1') ? 'true' : 'false';
    const mm = String(Math.floor(total / 60)).padStart(2, '0');
    const ss = String(total % 60).padStart(2, '0');
    return '<span class="mdp-mod-timer mdp-interactive" data-seconds="' + total + '" data-autostart="' + autostart + '">'
      + '<button class="mdp-timer-btn mdp-timer-start" type="button" title="Start / Pause">&#9654;</button>'
      + '<span class="mdp-timer-display">' + mm + ':' + ss + '</span>'
      + '<button class="mdp-timer-btn mdp-timer-reset" type="button" title="Reset">&#8634;</button>'
      + '</span>';
  ]]></render>

  <style><![CDATA[
    .mdp-mod-timer {
      display: inline-flex;
      align-items: center;
      gap: 0.4em;
      padding: 0.15em 0.55em;
      border-radius: 0.5em;
      background: rgba(0, 0, 0, 0.06);
      font-variant-numeric: tabular-nums;
      user-select: none;
      vertical-align: middle;
    }
    .mdp-mod-timer .mdp-timer-display { font-weight: 700; min-width: 2.8em; text-align: center; }
    .mdp-mod-timer.mdp-timer-done .mdp-timer-display { color: #ef4444; }
    .mdp-mod-timer .mdp-timer-btn {
      cursor: pointer; border: none; background: transparent; font: inherit;
      line-height: 1; padding: 0.1em 0.3em; border-radius: 0.3em; color: inherit;
    }
    .mdp-mod-timer .mdp-timer-btn:hover { background: rgba(0, 0, 0, 0.14); }
  ]]></style>

  <!--
    Per-instance interactive script with cross-surface state sync. The default
    export is an object with init(el, ctx). `ctx` provides:
      role          'owner' (runs the countdown) or 'mirror' (display only)
      getShared()   live shared state { remaining, running, done }
      setShared(p)  owner-only: merge + broadcast shared state to all surfaces
      onShared(cb)  render whenever the shared state changes (any surface)
      sendAction(t) dispatch a user action ('toggle' / 'reset') to the owner
      onAction(cb)  owner-only: handle dispatched actions
      presenting    true in the fullscreen slideshow (used for autostart)
      onCleanup(fn) clear the interval when the slide changes / role flips
    The owner runs a single setInterval and broadcasts the remaining seconds each
    tick; every surface (main screen, presenter, remote) renders the same state,
    and a click on any surface drives the owner through actions.
  -->
  <script><![CDATA[
    export default {
      init(el, ctx) {
        const display = el.querySelector('.mdp-timer-display');
        const startBtn = el.querySelector('.mdp-timer-start');
        const resetBtn = el.querySelector('.mdp-timer-reset');
        const total = parseInt(el.dataset.seconds, 10) || 300;

        const fmt = function (s) {
          s = Math.max(0, s);
          return String(Math.floor(s / 60)).padStart(2, '0') + ':' + String(s % 60).padStart(2, '0');
        };
        const paint = function (state) {
          const st = state || ctx.getShared() || { remaining: total, running: false, done: false };
          const remaining = (typeof st.remaining === 'number') ? st.remaining : total;
          display.textContent = fmt(remaining);
          startBtn.innerHTML = st.running ? '&#10073;&#10073;' : '&#9654;';
          el.classList.toggle('mdp-timer-done', !!st.done);
        };

        // Every surface re-renders on shared-state changes and routes clicks
        // through actions (the owner is the single source of truth).
        ctx.onShared(paint);
        startBtn.addEventListener('click', function () { ctx.sendAction('toggle'); });
        resetBtn.addEventListener('click', function () { ctx.sendAction('reset'); });

        if (ctx.role === 'owner') {
          let timer = null;
          const get = function () { return ctx.getShared() || { remaining: total, running: false, done: false }; };
          const stopInterval = function () { if (timer) { clearInterval(timer); timer = null; } };
          const startInterval = function () {
            if (timer) return;
            timer = setInterval(function () {
              const st = get();
              const next = (typeof st.remaining === 'number' ? st.remaining : total) - 1;
              if (next <= 0) { stopInterval(); ctx.setShared({ remaining: 0, running: false, done: true }); }
              else { ctx.setShared({ remaining: next }); }
            }, 1000);
          };

          // Initialise the shared state once (first owner to mount wins).
          if (!ctx.getShared()) ctx.setShared({ remaining: total, running: false, done: false });

          ctx.onAction(function (type) {
            const st = get();
            if (type === 'toggle') {
              if (st.running) { stopInterval(); ctx.setShared({ running: false }); }
              else if ((typeof st.remaining === 'number' ? st.remaining : total) > 0) {
                ctx.setShared({ running: true, done: false }); startInterval();
              }
            } else if (type === 'reset') {
              stopInterval(); ctx.setShared({ remaining: total, running: false, done: false });
            }
          });

          // Resume ticking if state says we are mid-run (owner re-mounted), then
          // honour autostart in the slideshow.
          if (get().running) startInterval();
          else if (ctx.presenting && el.dataset.autostart === 'true' && !get().done) {
            ctx.setShared({ running: true, done: false }); startInterval();
          }

          ctx.onCleanup(stopInterval);
        }

        paint();
      }
    }
  ]]></script>
</module>
